name: Audit

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  audit:
    name: Audit
    runs-on: ubuntu-latest
    container: cronoslabs/dev:v1.10.8
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3
      - name: Set rust toolchain
        run: rustup default bpf
      - name: Cargo build
        run: cargo build
      - name: Soteria audit
        run: soteria -analyzeAll .
